ktor:
  application:
    modules:
      - com.hugin_munin.ApplicationKt.module

  deployment:
    port: 8080

database:
  driver: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/hugin_munin_db
  user: postgres
  password: aurora28
  maxPoolSize: 10